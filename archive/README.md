# Archived work

`legacy/` preserves the previous pawn, queue/rank, and clock-budget approaches,
their documentation, and their incomplete proof. They are not part of the
active import graph. The current entry points are the repository-root
`LAWS.bend`, `PROOF.bend`, and `main.bend`.
